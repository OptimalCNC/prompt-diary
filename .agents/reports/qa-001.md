# QA Evidence Report

## Scope
- Assigned task: add end-to-end behavior tests for `prompt-diary generate` library and CLI behavior without editing production code.
- Files or areas inspected: `README.md`, `docs/src/prompt-diary-tool-design.md`, existing `src/prompt_diary/*` APIs for black-box invocation, existing tests, local Codex and Claude Code JSONL session shapes.
- Files changed, if any: `tests/test_prompt_diary_e2e_qa.py`; `.agents/reports/qa-001.md`.

## Requirements Checked
| Requirement | Evidence | Result |
| --- | --- | --- |
| Read and comply with README before changes | `README.md:5` documents Python 3.10+; `README.md:25` and `README.md:85`-`130` require `uv`, basedpyright, ruff, and pytest. | Pass |
| Read prompt diary design | `docs/src/prompt-diary-tool-design.md:33`-`48` define prepare/generate CLI behavior; `docs/src/prompt-diary-tool-design.md:70`-`72` require whole-session copy based on in-window events; `docs/src/prompt-diary-tool-design.md:206`-`240` define session indexes and target spans; `docs/src/prompt-diary-tool-design.md:349`-`365` define validation. | Pass |
| Study local Codex and Claude Code JSONL shapes without copying sessions | Shape-only `jq` inspections of `/home/huwei/.codex/sessions/...jsonl` showed Codex records with top-level `timestamp`, `type`, and `payload`, including `session_meta.payload.id/cwd` and `turn_context.payload.cwd`. Shape-only inspections of `/home/huwei/.claude/projects/...jsonl` and `subagents/agent-*.jsonl` showed top-level `timestamp`, `cwd`, `sessionId`, `uuid`, `message`, optional `toolUseResult`, no-timestamp metadata rows, and subagent `agentId`. No real transcript content was copied. | Pass |
| Reconstruct small similar Codex and Claude fixtures | Codex-like fixture preserves `session_meta`, `event_msg`, `turn_context`, `response_item`, top-level `timestamp`, payload timestamp/cwd, UUID-like filename pattern, and start/end boundary timestamps in `tests/test_prompt_diary_e2e_qa.py:267`-`314`. Claude-like fixture preserves no-timestamp `permission-mode`, top-level `cwd/sessionId/uuid`, `message`, `toolUseResult`, and timestamped user/assistant/system rows in `tests/test_prompt_diary_e2e_qa.py:336`-`430`. | Pass |
| Library E2E covers prepare, reuse, copy whole sessions, index spans, write and validate report | `tests/test_prompt_diary_e2e_qa.py:43`-`95` prepares, asserts workspace contents, re-prepares for reuse message, generates, checks validation, report header, and reuse messaging. Workspace assertions verify metadata, project, index rows, copied whole source files, and excluded outside-window session in `tests/test_prompt_diary_e2e_qa.py:215`-`264`. | Pass |
| Library E2E covers missing workspace generate auto-prepare | `tests/test_prompt_diary_e2e_qa.py:98`-`119` calls `generate_prompt_diary` without a workspace and asserts prepared workspace, `report.md`, validation, and prepare/write messages. | Pass |
| CLI E2E uses Typer CliRunner, isolated cwd, and env source-root overrides | `tests/test_prompt_diary_e2e_qa.py:122`-`155` and `tests/test_prompt_diary_e2e_qa.py:158`-`176` use `CliRunner`, `monkeypatch.chdir(tmp_path)`, and env overrides from `tests/test_prompt_diary_e2e_qa.py:433`-`437`. | Pass |
| CLI E2E covers prepare/generate and reuse/missing workspace behavior | CLI prepare plus existing-workspace generate reuse is asserted in `tests/test_prompt_diary_e2e_qa.py:130`-`154`; CLI generate auto-prepare from a missing workspace is asserted in `tests/test_prompt_diary_e2e_qa.py:166`-`175`. | Pass |
| No production code edits | Only `tests/test_prompt_diary_e2e_qa.py` and this report were changed by QA. Pre-existing `src/` and developer-test changes were already present in `git status --short` before QA edits and were not modified by QA. | Pass |

## Findings
| Severity | Finding | Evidence | Suggested action |
| --- | --- | --- | --- |
| Low | None found in the exercised prepare/generate behavior. | Focused QA tests passed: `tests/test_prompt_diary_e2e_qa.py .... [100%]`; full suite passed: `14 passed in 0.09s`. | None. |

## Verification
| Command or inspection | Evidence summary | Result |
| --- | --- | --- |
| `uv run pytest tests/test_prompt_diary_e2e_qa.py` | Final run: 4 tests collected, 4 passed in 0.05s. Initial sandboxed run was blocked by uv cache write access, then rerun with approved escalation. | Pass |
| `uv run ruff check` | Final output: `All checks passed!` | Pass |
| `uv run ruff format --check` | Initial output reported the new QA test file would be reformatted; after `uv run ruff format tests/test_prompt_diary_e2e_qa.py`, final output: `14 files already formatted`. | Pass |
| `uv run basedpyright` | Output: `0 errors, 0 warnings, 0 notes`. | Pass |
| `uv run pytest` | Full suite output: 14 tests collected and 14 passed in 0.09s. | Pass |
| Local session shape inspection | Observed Codex and Claude Code field/key/timestamp patterns with `jq` summaries only; no real session transcript content included in fixtures or this report. | Pass |

## Constraints Compliance
| Constraint | Evidence | Result |
| --- | --- | --- |
| Role boundary respected | QA added `tests/test_prompt_diary_e2e_qa.py` and `.agents/reports/qa-001.md` only; no `src/` production files were edited. | Pass |
| Evidence-backed report | Requirements, findings, and verification rows cite design/README/test file lines, command results, and explicit shape-inspection observations. | Pass |
| No real session copying | Reconstructed fixtures use synthetic content and IDs/timestamps only; real session inspections were shape-only summaries. | Pass |

## Residual Risk
- The implementation writes a deterministic conservative report rather than invoking an external report-writing model; these tests validate the current public library and CLI contract around workspace preparation, report writing, and validation, not model quality.
